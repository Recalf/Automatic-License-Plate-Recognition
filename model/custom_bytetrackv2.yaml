tracker_type: bytetrack

track_high_thresh: 0.4 # higher than track_low
track_low_thresh: 0.25 # same as yolo's confidence
new_track_thresh: 0.5 # higher than all

track_buffer: 90
match_thresh: 0.8

fuse_score: True